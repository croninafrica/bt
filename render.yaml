# Render 一键部署: https://render.com
# Dashboard → New → Blueprint → 连接本仓库
services:
  - type: web
    name: magnet-search
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
